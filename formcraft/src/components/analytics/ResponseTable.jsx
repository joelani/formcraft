// ResponseTable.jsx
// Phase 5 - Analytics
// Renders the tabular response list for a form.

export default function ResponseTable({ form, submissions }) {
  return <div>ResponseTable — coming in Phase 5</div>
}
