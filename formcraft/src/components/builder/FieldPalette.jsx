// FieldPalette.jsx
// Phase 3 - Form Builder
// Renders available field types users can drag onto the builder canvas.

export default function FieldPalette({ formId }) {
  return <div>FieldPalette — coming in Phase 3</div>
}
