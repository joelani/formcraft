// PropertiesPanel.jsx
// Phase 3 - Form Builder
// Renders controls for the selected field's editable properties.

export default function PropertiesPanel({ fieldId }) {
  return <div>PropertiesPanel — coming in Phase 3</div>
}
