// CheckboxField.jsx
// Phase 3 - Form Builder
// Renders a checkbox group field preview and public form control.

export default function CheckboxField({ field, value, onChange }) {
  return <div>CheckboxField — coming in Phase 3</div>
}
