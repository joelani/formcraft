// DropdownField.jsx
// Phase 3 - Form Builder
// Renders a dropdown field preview and public form control.

export default function DropdownField({ field, value, onChange }) {
  return <div>DropdownField — coming in Phase 3</div>
}
