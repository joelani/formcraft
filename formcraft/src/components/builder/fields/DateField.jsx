// DateField.jsx
// Phase 3 - Form Builder
// Renders a date field preview and public form control.

export default function DateField({ field, value, onChange }) {
  return <div>DateField — coming in Phase 3</div>
}
