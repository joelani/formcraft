// EmailField.jsx
// Phase 3 - Form Builder
// Renders an email field preview and public form control.

export default function EmailField({ field, value, onChange }) {
  return <div>EmailField — coming in Phase 3</div>
}
