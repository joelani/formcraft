// TextField.jsx
// Phase 3 - Form Builder
// Renders a single-line text field preview and public form control.

export default function TextField({ field, value, onChange }) {
  return <div>TextField — coming in Phase 3</div>
}
