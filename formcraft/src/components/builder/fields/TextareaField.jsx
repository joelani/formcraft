// TextareaField.jsx
// Phase 3 - Form Builder
// Renders a multi-line text field preview and public form control.

export default function TextareaField({ field, value, onChange }) {
  return <div>TextareaField — coming in Phase 3</div>
}
