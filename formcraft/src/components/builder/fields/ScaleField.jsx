// ScaleField.jsx
// Phase 3 - Form Builder
// Renders a rating scale field preview and public form control.

export default function ScaleField({ field, value, onChange }) {
  return <div>ScaleField — coming in Phase 3</div>
}
